# Handoff Layout Proof Record Template

**Packet:** WF-RV-PILOT-001 version 1.2.5
**Status:** Facilitator-only blank template; no layout result exists

After the completed Markdown handoff is manifested and verified, render its
exact bytes and export this run-specific record as
`WF-A-HANDOFF-LAYOUT-PROOF-<attempt-id>-v1.md`. Preserve the PDF separately as
`WF-A-ONE-SCREEN-HANDOFF-v1.pdf`.

## Exact identity

- Attempt ID:
- Markdown filename: `WF-A-ONE-SCREEN-HANDOFF-v1.md`
- Markdown SHA-256:
- PDF filename: `WF-A-ONE-SCREEN-HANDOFF-v1.pdf`
- PDF SHA-256:
- Proof record filename:
  `WF-A-HANDOFF-LAYOUT-PROOF-<attempt-id>-v1.md`
- Artifact ID/version: `WF-A-HANDOFF-LAYOUT-PROOF/v1`
- Completion timestamp/timezone:
- State: `LAYOUT PROOF COMPLETE` / invalid

## Reproducible rendering evidence

- Literal rendering command:
- Renderer and exact version:
- Supporting tool versions:
- Page size: `US Letter portrait` / invalid
- Page count: `1` / invalid
- Top/right/bottom/left margins in inches:
- Minimum body text size in points:
- Minimum table text size in points:
- Reader-facing word count:
- Immutable provenance word count excluded from the reader-facing count:
- Clipping observed: no / invalid
- Overlap observed: no / invalid
- Hidden overflow observed: no / invalid
- Unreadable shrinking observed: no / invalid
- Visual inspection actor and timestamp/timezone:
- Layout result: `PASS` / `FAIL`

`PASS` requires exactly one US Letter portrait page, every margin at least
0.5 inch, body and table text at least 9 points, no more than 450 reader-facing
words excluding only the handoff's labeled immutable provenance metadata, and
no clipping, overlap, hidden overflow, or unreadable shrinking. Preserve a
two-page or compressed result as `FAIL`; its literal one-page disposition is
`HOLD`. Missing proof also means no favorable one-page claim.

Layout proof is not comprehension evidence. A local PASS does not establish
that a person can scan, understand, trust, or use the handoff.
